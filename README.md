# biasRecords static site

This is a lightweight static version of biasRecords.

Open `index.html` directly in a browser, or upload the full folder to a static host such as Cloudflare Pages, Netlify, GitHub Pages, or a regular rental server.
